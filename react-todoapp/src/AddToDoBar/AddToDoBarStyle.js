import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container : {
        flex : 1,
    },
    textInput : {
        backgroundColor : "#8a8bbf",
        margin : 10,
        opacity : 0.3,
        color : "black",
    }
})

export default styles;